
# Настройка рабочего окружения для изучения SQL Oracle и подготовки к экзамену 1Z0-071

*Полное руководство по установке Oracle Database 21c XE, SQL Developer и настройке учебной среды на Oracle Linux 8*

## 📌 Для кого этот гайд
- Студенты, готовящиеся к экзамену 1Z0-071 (Oracle Database SQL)
- Разработчики, которым нужна локальная Oracle БД для практики
- Те, кто хочет использовать Oracle Linux как рабочую 

## 🧪 Где это проверено (мой тестовый стенд)
*Все инструкции проверены лично на следующем оборудовании:*
| Платформа | Конфигурация | Статус |
|-----------|--------------|--------|
| **ASUS UX303LN** | i7-4510U, 8GB RAM, Oracle Linux 8.10 (чистая установка) | ✅ Работает |
| **ThinkPad X240** | i7-4600U, 8GB RAM, Oracle Linux 8.10 (обновленная система) | ✅ Работает |
| **HP EliteBook 840 G3** | i5-6300U, 16GB RAM, Oracle Linux 8.10 (чистая установка) | ✅ Работает |
| **VirtualBox на iMac'2012** | 4 ядра, 8GB RAM, гостевая OL8.10 | ✅ Работает |
| **VirtualBox на Mac mini'2012** | 4 ядра, 8GB RAM, гостевая OL8.10 | ✅ Работает |

<details>
<summary>📋 Полные спецификации (neofetch)</summary>
</details>

> 💡 **Важно**: Все настройки делаются локально, никаких "удаленных серверов" — только ваше железо.

---
## 🔧 Системные требования
- **ОС**: Oracle Linux 8 (проверено на версии 8.10)
- **RAM**: минимум 4 ГБ (рекомендуется 8 ГБ для комфортной работы)
- **Диск**: минимум 16 ГБ свободного места
- **Java**: OpenJDK 1.8 или выше (установим позже)

> ⚠️ **Важно**: Инструкция проверена только на Oracle Linux 8. 
> На других дистрибутивах (RHEL, AlmaLinux, Rocky Linux) не тестировалась — 
> возможны отличия в настройках и пакетах.
---

## 📦 Подготовка системы

### 1. Проверяем имя хоста (важно для listener'а)
```bash
# Проверяем текущее имя
$ hostname

# Если имя отличается от localhost — правим hosts
$ sudo nano /etc/hosts
```
Добавьте или раскомментируйте:
```
127.0.0.1   localhost localhost.localdomain
127.0.0.1   oracle8  oracle8.me   # замените на ваше имя хоста
```

### 2. Обновляем систему и ставим базовые пакеты
```bash
$ cd ~
$ sudo dnf update -y
$ sudo dnf install wget nano -y
```

---

## 🗄️ Установка Oracle Database 21c XE

### 3. Устанавливаем предварительные зависимости
```bash
$ sudo dnf install oracle-database-preinstall-21c -y
```

### 4. Скачиваем и устанавливаем БД
```bash
# Скачиваем RPM-пакет
$ wget https://download.oracle.com/otn-pub/otn_software/db-express/oracle-database-xe-21c-1.0-1.ol8.x86_64.rpm

# Устанавливаем
$ sudo dnf install oracle-database-xe-21c-1.0-1.ol8.x86_64.rpm -y
```

### 5. Запускаем сервис и добавляем в автозагрузку
```bash
$ sudo systemctl enable oracle-xe-21c
$ sudo systemctl start oracle-xe-21c
$ sudo systemctl status oracle-xe-21c   # проверяем, что всё ок
```

---

## 🔑 Первоначальная настройка БД

### 6. Настраиваем переменные окружения для пользователя
```bash
$ nano ~/.bash_profile
```
Добавьте в конец файла:
```bash
export ORACLE_HOME=/opt/oracle/product/21c/dbhomeXE
export ORACLE_SID=XE
export PATH=$ORACLE_HOME/bin:$PATH
```
Применяем изменения:
```bash
$ source ~/.bash_profile
```

### 7. Конфигурируем базу (задаем пароль)
```bash
$ sudo /etc/init.d/oracle-xe-21c configure
```
В процессе установки вас попросят задать пароль для системных пользователей (SYS, SYSTEM, PDBADMIN). **Запомните его!**

### 8. Добавляем пользователя в группу dba
```bash
$ sudo usermod -aG dba $USER
# Выходим из системы и заходим снова, чтобы применить группы
# или выполняем:
$ newgrp dba
```

### 9. Проверяем подключение к БД
```bash
$ sqlplus / as sysdba
```
---

## 👤 Создаем учебного пользователя

### 10. Переключаемся в нужный контейнер (PDB)
```sql
SQL> SHOW CON_NAME;   -- должно быть CDB$ROOT
SQL> ALTER SESSION SET CONTAINER = XEPDB1;
SQL> SHOW CON_NAME;   -- теперь XEPDB1
```

### 11. Создаем пользователя student
```sql
SQL>CREATE USER student IDENTIFIED BY student
  DEFAULT TABLESPACE users
  TEMPORARY TABLESPACE temp
  QUOTA UNLIMITED ON users;
```

### 12. Даем необходимые привилегии
```sql
-- Базовые права для подключения и работы
SQL>GRANT CONNECT, RESOURCE TO student;
-- или более современный вариант:
SQL>GRANT CREATE SESSION TO student;

-- Права на создание объектов
SQL>GRANT CREATE VIEW, CREATE SEQUENCE, CREATE PROCEDURE TO student;
SQL>GRANT CREATE TRIGGER TO student;  -- если нужно

-- Выходим
SQL>EXIT
```

### 13. Проверяем подключение под student
```bash
SQL>sqlplus student/student@localhost:1521/XEPDB1
```

### 14. Создаем тестовую таблицу
```sql
SQL>CREATE TABLE test_table (
  id NUMBER PRIMARY KEY,
  name VARCHAR2(50),
  created_at DATE DEFAULT SYSDATE
);

SQL>INSERT INTO test_table (id, name) VALUES (1, 'Ivan');
SQL>INSERT INTO test_table (id, name) VALUES (2, 'Maria');
SQL>INSERT INTO test_table (id, name) VALUES (3, 'Antonio');

SQL>COMMIT;

SQL>SELECT * FROM test_table;

SQL>EXIT;
```

---

## ☕ Установка Java и SQL Developer

### 15. Ставим Java 17 (рекомендуется для последних версий SQL Developer)
```bash
$ sudo dnf install java-17-openjdk java-17-openjdk-devel -y

# Проверяем версию по умолчанию
$ sudo alternatives --config java   # выбираем 17, если нужно

# Прописываем JAVA_HOME
$ echo 'export JAVA_HOME=/usr/lib/jvm/java-17-openjdk' >> ~/.bashrc
$ echo 'export PATH=$JAVA_HOME/bin:$PATH' >> ~/.bashrc
$ source ~/.bashrc

# Проверяем
$ java --version
```

### 16. Скачиваем и устанавливаем SQL Developer
```bash
$ wget https://download.oracle.com/otn_software/java/sqldeveloper/sqldeveloper-24.3.1-347.1826.noarch.rpm
$ sudo dnf install sqldeveloper-24.3.1-*.rpm -y
```

### 17. Запускаем (проверка)
```bash
$ sqldeveloper
```
## 🔌 Настройка подключения в SQL Developer
1. Нажмите зеленый **"+"** (New Connection)
2. Заполните поля:

| Поле | Значение |
|------|----------|
| **Connection Name** | XE Student (можно любое) |
| **Username** | student |
| **Password** | student |
| **Save Password** | ✓ (по желанию) |
| **Connection Type** | Basic |
| **Hostname** | localhost (или IP вашей машины) |
| **Port** | 1521 |
| **Service Name** | XEPDB1 |

4. Нажмите **Test** — должно показать "Success"
5. Нажмите **Save**, затем **Connect**

### Проверка
В открывшемся окне SQL Developer выполните:
```sql
SQL>SELECT * FROM test_table;
```
Нажмите зеленую стрелочку (F5). Должны увидеть:
```
ID | NAME    | CREATED_AT
---|---------|------------------
1  | Ivan    | 27-FEB-26
2  | Maria   | 27-FEB-26
3  | Antonio | 27-FEB-26
```

---

## 🐛 Возможные ошибки и их решение

### ❌ ORA-12541: TNS:no listener

**Симптом**: При подключении `sqlplus student/student@localhost:1521/XEPDB1` получаем ошибку.

**Решение**:

1. Переключитесь на пользователя oracle:
```bash
$ sudo su - oracle
```

2. Проверьте настройки listener'а:
```bash
# Добавляем переменные для пользователя oracle (если еще не добавлены)
$ echo "export ORACLE_HOME=/opt/oracle/product/21c/dbhomeXE" >> ~/.bash_profile
$ echo "export ORACLE_SID=XE" >> ~/.bash_profile
$ echo "export PATH=$ORACLE_HOME/bin:$PATH" >> ~/.bash_profile
$ source ~/.bash_profile

# Смотрим статус
$ lsnrctl status
```

3. **Важно**: Обратите внимание на PORT в выводе! У меня он был **1539**, а не 1521:
```
Listening Endpoints Summary...
  (DESCRIPTION=(ADDRESS=(PROTOCOL=tcp)(HOST=ol8)(PORT=1539)))
```

4. Смотрим конфиг listener.ora:
```bash
$ cat $ORACLE_HOME/network/admin/listener.ora
```

5. Если порт отличается, подключаемся через него:
```bash
$ sqlplus student/student@localhost:1539/XEPDB1   # вместо 1521
```

6. Или перезапускаем listener:
```bash
$ lsnrctl stop
$ lsnrctl start
# или
$ lsnrctl reload
```

> 💡 **Совет**: Если вы меняете порт в listener.ora, не забудьте обновить подключения в SQL Developer и перезапустить listener!

---

## ✅ Что дальше?

Теперь у вас полностью рабочая среда для:
- Подготовки к экзамену 1Z0-071
- Изучения SQL Oracle
- Разработки приложений с Oracle БД

Все работает локально, без интернета, на вашем железе. Семья может продолжать пользоваться компьютером для обычных дел — Oracle Linux отлично справляется с ролью рабочей станции.

---

## 📚 Полезные ссылки
- [Официальная документация Oracle Database 21c XE](https://docs.oracle.com/en/database/oracle/oracle-database/21/xeinl/)
- [Спецификация экзамена 1Z0-071](https://education.oracle.com/oracle-database-sql/pexam_1Z0-071)
- [SQL Developer документация](https://docs.oracle.com/en/database/oracle/sql-developer/)

---

*Нашли ошибку? Есть вопросы? Открывайте issue в репозитории или пишите в обсуждения!*
```


